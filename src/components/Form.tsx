import { FormContainer, FormGroup, Title } from '../components';

const Form = () => {
  return (
    <FormContainer>
      <Title>Dealt-With</Title>
      <FormGroup />
    </FormContainer>
  );
};

export default Form;
